# def add_ten(num):
#     return num + 10

# By default a lambda function will automatically return the result so we can omit the word 'return'. Lambda functions are called 'anonymous functions'.
add_ten_lambda = lambda num: num + 10

print(add_ten_lambda(5))